App\Entity\User:
  admin:
    email: admin@email.fr
    roles: ['ROLE_ADMIN']
    password: '<encodePassword(admin)>'

  user:
    email: user@email.fr
    roles: ['ROLE_USER']
    password: '<encodePassword(user)>'
