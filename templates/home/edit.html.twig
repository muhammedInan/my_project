{% extends "base.html.twig" %}

{% block body %}
<div class="container">
    <div class="section">

        <div class="row">
            <div class="col s12 m12">

               <h1>modifier de {{car.model }}</h1>
               {% include 'home/form.html.twig' %}

               {{ form_start(form) }}
               <button class="btn waves-effect waves-light" type="submit" name="action">editer</button>

               {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}